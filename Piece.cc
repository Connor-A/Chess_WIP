#include <string>
#include <"Piece.h">

using namespace std;

piece::piece()
{
	team = NULL;
	has_moved = NULL;
	type = "SPACE"
}

bool piece::is_legal(string move);
{
	return NULL;
}

bool make_move(string move);
{
	return NULL;
}