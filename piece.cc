#include <string>
#include "Piece.h"

using namespace std;

piece::piece()
{
	team = NULL;
	has_moved = NULL;
	type = "SPACE";
}

void piece::is_legal(string move)
{
}

void make_move(string move)
{
}